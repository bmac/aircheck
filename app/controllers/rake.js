import chat from 'aircheck/helpers/chat';

var RoomController = Ember.Controller.extend({
    // actions: {
    //     sendMessage: function(msg) {
    //         console.log(arguments);
    //         console.log(this.get('msg'));
    //         chat.sendAll(msg);
    //         this.set('msg', '');
    //     }
    // }
});

export default RoomController;
