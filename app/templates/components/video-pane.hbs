<section class="peers">
    {{#each peers}}
    <div class="peer">
        <label class="other-label">{{ nick }}</label>
        <video {{bindAttr src=videoSrc }} autoplay class="other"></video>
    </div>
    {{/each}}
</section>
